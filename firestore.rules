/**
 * This ruleset is now simplified as most data has been migrated to Supabase.
 * Firestore is no longer used for storing user profiles, donation claims, or match data.
 * These rules are kept as a placeholder but do not actively secure critical data,
 * as that is handled by Supabase policies or application logic.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All paths are locked down by default as data is now in Supabase.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
